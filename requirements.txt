# Core scientific stack
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
scipy>=1.11.0

# Deep learning (CPU-safe; GPU auto-used if CUDA available)
tensorflow>=2.13.0
torch>=2.1.0
torchvision>=0.16.0

# Computer vision
opencv-python>=4.8.0
Pillow>=10.0.0
ultralytics>=8.0.0         # YOLOv8

# Visualization & notebooks
matplotlib>=3.7.0
seaborn>=0.12.0
jupyter>=1.0.0
ipykernel>=6.25.0

# Utilities
tqdm>=4.66.0
pytest>=7.4.0

# Optional: CAN signal simulation (only needed if using CARLA simulator)
# carla>=0.9.14            # Uncomment only if CARLA server is installed